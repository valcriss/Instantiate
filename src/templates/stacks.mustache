<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Instantiate</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; }
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 0.5rem; text-align: left; }
    th { background: #f5f5f5; }
    td ul { list-style:none; }
    tr:nth-child(even) { background-color: #f9f9f9; }
    .tag { padding: 0.2em 0.5em; border-radius: 4px; font-size: 0.9em; }
    .github { background: #eaf5ff; color: #0366d6; }
    .gitlab { background: #fdecef; color: #e24329; }
    .running { background: #e6ffed; color: #22863a; }
    .stopped { background: #fff5e6; color: #735c0f; }
    .error { background: #ffeef0; color: #d73a49; }
  </style>
</head>
<body>
  <h1>🚀 Active Stacks</h1>
  <table>
    <thead>
      <tr>
        <th>Project</th>
        <th>Merge Request</th>
        <th>Provider</th>
        <th>Status</th>
        <th>Links</th>
        <th>Created</th>
        <th>Updated</th>
      </tr>
    </thead>
    <tbody>
      {{#stacks}}
      <tr>
        <td>{{projectName}}</td>
        <td>{{mergeRequestName}}</td>
        <td><span class="tag {{provider}}">{{provider}}</span></td>
        <td><span class="tag {{status}}">{{status}}</span></td>
        <td>
          <ul>
            {{#links}}
              <li>🔗 <a href="{{url}}" target="_blank">{{name}}</a></li>
            {{/links}}
          </ul>
        </td>
        <td>{{createdAt}}</td>
        <td>{{updatedAt}}</td>
      </tr>
      {{/stacks}}
    </tbody>
  </table>
</body>
</html>
